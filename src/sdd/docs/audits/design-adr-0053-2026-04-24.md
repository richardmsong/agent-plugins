## Run: 2026-04-24T00:00:00Z

**Gaps found: 6**

1. **New spec-evaluator skill invocation protocol underspecified** — The ADR says the new `/spec-evaluator` skill "takes an optional component argument" and "when invoked without arguments from `/plan-feature` or `/feature-change`, evaluates the specific ADR being processed against all specs it references." However it never specifies what argument is passed when invoked *with* an argument from the loop, what the invocation call looks like (compare to `/spec-evaluator` SKILL.md which shows `Agent({subagent_type:...})`), or how the calling skill passes the ADR path to the new skill. A developer writing the new SKILL.md cannot know the expected prompt structure.
   - **Doc**: "New skill: `.agent/skills/spec-evaluator/` — New SKILL.md that invokes the new `spec-evaluator` agent (ADR-to-spec alignment). Same pattern as `/implementation-evaluator`: takes an optional component argument, runs agent, reports CLEAN or gap list. When invoked without arguments from `/plan-feature` or `/feature-change`, evaluates the specific ADR being processed against all specs it references"
   - **Code**: Existing `spec-evaluator/SKILL.md` shows the invocation pattern takes a component name and passes it as `subagent_type: "spec-evaluator"` with a prompt containing component root and spec paths. The new skill serves a fundamentally different purpose (ADR path, not component) — the "same pattern" claim is incomplete without specifying what the prompt looks like.

2. **Gap direction `UNCLEAR` handling undefined in the master session loop** — The loop mechanic in both `/plan-feature` Step 5b and `/feature-change` Step 4b says "if gaps: master fixes specs, go to (b/a)." The spec-evaluator agent can emit three directions: `SPEC→FIX`, `ADR→FIX`, and `UNCLEAR`. The ADR never says what the master session does when it receives `UNCLEAR` — does it ask the user via `AskUserQuestion`? Does it make a judgment call? This is a blocking question for implementing the loop logic.
   - **Doc**: "Gap directions: `SPEC→FIX` (spec missing something the ADR decided) or `ADR→FIX` (ADR contradicts itself or the spec is actually right)"; loop says "If gaps: master fixes specs, go to (b)"
   - **Code**: The analogous pattern in `feature-change/SKILL.md` Step 6 has explicit handling for each direction type including `UNCLEAR` ("flag for the caller to decide"). The new loop mechanic omits this.

3. **`ADR→FIX` handling in the loop is self-contradicting** — The Error Handling table says "Direction: `ADR→FIX`. Master fixes the ADR, re-runs evaluator." But the loop mechanic shows only one action path: "master fixes specs." The ADR never specifies the loop step for the case where the evaluator says the ADR is wrong — does the master edit the ADR (violating the plan-feature write rules for already-accepted ADRs), ask the user, or author a superseding ADR?
   - **Doc**: "Spec-evaluator finds gap that is actually an ADR error → Direction: `ADR→FIX`. Master fixes the ADR, re-runs evaluator." vs "If gaps: master fixes specs, go to (b)"
   - **Code**: `plan-feature/SKILL.md` doc editing rules state "ADR content is historical. Do not rewrite past decisions." At Step 5b the ADR is still in `draft` status (pre-commit), so editing it may be valid — but this is not stated. In `/feature-change` Step 4b the ADR is already `accepted` and committed, making "fix the ADR" a direct violation of the immutability rule without further guidance.

4. **`Glob("docs/**/spec-*.md")` scope in Phase 0 does not match the decision text** — The Phase 0 algorithm says `Glob("docs/**/spec-*.md")` to find specs referenced by the ADR, but the Decisions table says "Evaluator checks all specs that the ADR's Impact section lists, plus any spec the ADR's Component Changes section implies." These are not the same set. A developer implementing the agent must choose: glob everything and filter, or read Impact/Component Changes to enumerate. The algorithm as written always reads all specs, but the decision description implies targeted reading.
   - **Doc**: Phase 0 step 3: `Glob("docs/**/spec-*.md")` — read all specs referenced by the ADR." Decision row: "Evaluator checks all specs that the ADR's Impact section lists, plus any spec the ADR's Component Changes section implies."
   - **Code**: No code exists yet; the inconsistency is within the ADR itself.

5. **Audit file naming collision between new and renamed agents** — The renamed agent (implementation-evaluator) currently saves to `.agent/audits/spec-<component>-<YYYY-MM-DD>.md` (per existing `spec-evaluator.md` line 206). The new spec-evaluator agent saves to `.agent/audits/spec-alignment-<slug>-<YYYY-MM-DD>.md` (Component Changes section). The ADR does not specify whether the renamed agent changes its audit filename prefix from `spec-` to something else (e.g. `impl-`). If both agents run on the same day for the same component, a name collision is possible only if the old prefix is kept. A developer updating the implementation-evaluator agent file cannot know whether to rename the audit output path.
   - **Doc**: "Saves results to `.agent/audits/spec-alignment-<slug>-<YYYY-MM-DD>.md`" (new agent); "File renamed, `name:` field in frontmatter updated / All internal references to 'spec-evaluator' in the agent description updated to 'implementation-evaluator' / No behavioral changes — same algorithm, same output format" (renamed agent)
   - **Code**: `spec-evaluator.md:206` — audit path is `spec-<component>-<YYYY-MM-DD>.md`. Existing `feature-change/SKILL.md:190` references `/spec-evaluator <component>` — after rename this becomes `/implementation-evaluator <component>`, but the audit path also changes if the prefix changes. "No behavioral changes" conflicts with a necessary filename change.

6. **`context.md` is not a spec file and its update is not described** — The Impact section lists `src/sdd/context.md` as a file to update. The Component Changes section says "Any references to 'spec-evaluator' in the SDD workflow description updated to 'implementation-evaluator' / New spec-evaluator loop mentioned in the pipeline description." However `context.md` contains no mention of `spec-evaluator` anywhere in its current text, and its "pipeline description" is a one-line summary (`/feature-change` reads specs → classifies → authors ADR → updates spec if needed → commits spec → calls dev-harness → implements and tests). A developer cannot know what text to add for "new spec-evaluator loop mentioned in the pipeline description" — the ADR provides no example text, no placement guidance, and no indication of what level of detail is expected.
   - **Doc**: "Updated context.md — Any references to 'spec-evaluator' in the SDD workflow description updated to 'implementation-evaluator'. New spec-evaluator loop mentioned in the pipeline description."
   - **Code**: `/Users/270840341/work/claude-plugins/src/sdd/context.md` — the file contains no `spec-evaluator` string anywhere and has a one-line pipeline summary. There is nothing to rename and no obvious insertion point for the loop description.

## Run: 2026-04-24T00:00:00Z

**Gaps found: 6**

1. **First-time detection: `spec-driven-config.json` absent is wrong signal** — The ADR says "detects existing config" by checking whether `spec-driven-config.json` exists. But the current SKILL.md Step 1 already writes `spec-driven-config.json` unconditionally on every first-time run and skips it (printing a message) if it exists. A user who ran `/setup` previously, deleted that file, and re-ran would incorrectly get the tutorial. More critically, a user whose first `/setup` run was interrupted after `spec-driven-config.json` was written but before the tutorial was printed will never see the tutorial. The ADR gives no authoritative definition of "first-time user" — no fallback signal (e.g. check for `CLAUDE.md` markers, or check whether sdd-master symlink exists) and no tie-breaking rule when signals conflict.
   - **Doc**: "Detects `spec-driven-config.json` already exists" and "Tutorial only shows on first run"
   - **Code**: `claude/sdd/skills/setup/SKILL.md` lines 59–82 — config is written in Step 1, which runs before any tutorial output, so by the time the algorithm reaches the tutorial check, config always exists.

2. **Algorithm step numbering is internally inconsistent** — The Component Changes algorithm (under "setup SKILL.md") lists 9 steps numbered 1–9. The current SKILL.md has 5 named sections labeled "Step 1" through "Step 5" (with "Step 1 — Write spec-driven-config.json" despite the algorithm placing "Resolve PLATFORM_ROOT" as step 1). The ADR inserts tutorial steps around the existing mechanical steps but does not say whether the named `## Step N` section headers in SKILL.md are renumbered, or whether new tutorial sections are added as unnumbered prose blocks, or some other structure. A developer does not know what the rewritten SKILL.md's section structure looks like.
   - **Doc**: Component Changes section lists steps 1–9 but doesn't specify how they map to the existing `## Step 1` … `## Step 5` header structure in the SKILL.md
   - **Code**: `claude/sdd/skills/setup/SKILL.md` lines 57–175 — five `## Step N` sections exist; their renaming (or not) after inserting tutorial content is unspecified.

3. **JSONL sampling: no bound on files read, no privacy handling specified** — The ADR instructs Claude to "sample a few JSONL files from `~/.claude/projects/`" to infer what the user has been working on in other projects. It does not specify: how many files to read, how many lines per file, whether to skip files from sensitive-looking directories (e.g. `/private/`, banking repos), what to do if no JSONL files are parseable or readable (beyond the generic "Can't read repo context → skip" error row, which is repo-context-only). The `~/.claude/projects/` directory exists and contains JSONL conversation logs. A developer does not know the sampling strategy — breadth vs recency, line count limit, or how to extract "what they've been working on" from raw JSONL structure.
   - **Doc**: "sample a few JSONL files from `~/.claude/projects/`" with no further specification of how many, which ones, or how to parse them
   - **Code**: `~/.claude/projects/` exists with project-keyed subdirectories each containing `.jsonl` conversation files — structure confirmed, but JSONL schema is undocumented in this ADR.

4. **Returning-user path omits sdd-master symlink refresh** — The returning user flow (User Flow §2) says setup "silently refreshes CLAUDE.md … and permissions" and prints a one-liner. The current SKILL.md Step 4 always re-runs `ln -sf` for the sdd-master symlink (it's idempotent). The ADR's returning-user description does not mention the symlink step — a developer doesn't know whether to skip it (tutorial only) or run it (idempotent refresh). If the symlink is skipped on re-run, a user who installed the plugin to a new location and re-ran `/setup` would have a stale symlink.
   - **Doc**: "Silently refreshes CLAUDE.md (in case context.md was updated) and permissions" — symlink not mentioned
   - **Code**: `claude/sdd/skills/setup/SKILL.md` line 144 — symlink step is unconditional today.

5. **Skills overview list is incomplete and naming diverges from actual skills** — The skills overview block in User Flow step 6 lists four skills: `/feature-change`, `/plan-feature`, `/dashboard`, `/spec-evaluator`. There is also a `/design-audit` skill and a `/file-bug` skill in `claude/sdd/skills/`. The ADR does not say whether to include or exclude those skills, or why. Additionally the ADR lists `/dashboard` but the installed skill name is `dashboard` (not prefixed with `/`); whether the overview block uses `/skill-name` or `skill-name` notation is also inconsistent with how the current SKILL.md header describes invocation (`/setup`, no prefix in usage block). A developer doesn't know whether to audit the list against the actual skills directory or trust the ADR's enumeration.
   - **Doc**: Skills overview block enumerates four skills without explaining what criteria govern inclusion
   - **Code**: `claude/sdd/skills/` contains six skill directories: `dashboard`, `design-audit`, `feature-change`, `file-bug`, `plan-feature`, `spec-evaluator` — two are absent from the list.

6. **Error table is incomplete: no handling for JSONL-branch failures** — The Error Handling table covers only two cases: "Can't read repo context" (skip, generic guidance) and "User interrupts tutorial" (no harm). The JSONL-sampling path introduces additional failure modes — `~/.claude/projects/` doesn't exist, all sampled files are empty or unreadable, JSONL content is parseable but yields no clear task signal. None of these are in the error table, yet the ADR treats the JSONL path as a primary branch of the contextual example logic (not a sub-case of "Can't read repo context"). A developer implementing the JSONL branch doesn't know whether JSONL parse failures collapse into the "Can't read repo context" handler or trigger their own fallback.
   - **Doc**: Error Handling table has two rows; "Can't read repo context" appears to be repo-only ("Skip contextual example; print generic guidance instead") without explicit coverage of the JSONL sampling sub-path
   - **Doc**: User Flow step 5 branches into three cases (repo has code / empty repo with JSONL / no history) but Error Handling does not mirror this three-way structure.

## Run: 2026-04-24T00:00:00Z

**Gaps found: 6**

1. **Hook wrappers still reference `../../../src/sdd/hooks/guards/` — broken after install** — The ADR marks `blocked-commands-hook.sh` and `source-guard-hook.sh` as "real (unchanged)" in the output directory tree. But the actual files at `claude/sdd/hooks/blocked-commands-hook.sh` (line 4) and `claude/sdd/hooks/source-guard-hook.sh` (line 4) hardcode `GUARD="$SCRIPT_DIR/../../../src/sdd/hooks/guards/blocked-commands.sh"` and the equivalent for source-guard. That path walks up from `claude/sdd/hooks/` to find `src/sdd/` — which does not exist on the installed machine. The guards are correctly copied to `${CLAUDE_PLUGIN_ROOT}/hooks/guards/`, and the decision table says "Hook wrappers reference `${CLAUDE_PLUGIN_ROOT}/hooks/guards/`", but the ADR does not specify how the wrappers are rewritten to use that path rather than the relative `../../../src/sdd/` path. A developer implementing this does not know: does `build.sh` sed-replace the path? Are the wrappers regenerated from a template? Are they rewritten to use `${CLAUDE_PLUGIN_ROOT}` instead of `$SCRIPT_DIR`?
   - **Doc**: "blocked-commands-hook.sh — real (unchanged)" / "Hook wrappers reference `${CLAUDE_PLUGIN_ROOT}/hooks/guards/`; files just need to exist there"
   - **Code**: `claude/sdd/hooks/blocked-commands-hook.sh:4` — `GUARD="$SCRIPT_DIR/../../../src/sdd/hooks/guards/blocked-commands.sh"`. Same pattern in `source-guard-hook.sh:4`.

2. **`import.meta.dir` in bundled dashboard server resolves to the build machine's source path** — `server.ts` (line 153) sets `const UI_DIST = join(import.meta.dir, "../ui/dist")` and (line 192) uses `cwd: join(import.meta.dir, "../ui")` for the auto-build spawn. When Bun bundles this file, `import.meta.dir` is statically replaced with the source directory at build time (`src/sdd/docs-dashboard/src`). On the end user's machine that path does not exist, so both the UI static file serving and the auto-build fallback are broken. The ADR acknowledges the problem ("the server should check `${CLAUDE_PLUGIN_ROOT}/dist/ui/` for the pre-built UI") but does not specify the concrete fix: which env var or argv does the bundled server use instead of `import.meta.dir`? Does the build script inject a `--define` override? Does server.ts need a code change before bundling?
   - **Doc**: "This means the dashboard server needs to resolve its UI dist path relative to the bundle location, not `import.meta.dir`. The build step or server code must handle this — the server should check `${CLAUDE_PLUGIN_ROOT}/dist/ui/` for the pre-built UI."
   - **Code**: `src/sdd/docs-dashboard/src/server.ts:153,192` — uses `import.meta.dir` with no fallback to `CLAUDE_PLUGIN_ROOT`.

3. **`bun build` for docs-dashboard is run from `docs-dashboard/` without a prior `bun install`** — `docs-dashboard` has a `workspace:*` dependency on `docs-mcp` (`docs-dashboard/package.json`). The build script runs `bun install --frozen-lockfile` only inside `docs-mcp/`, then CDs into `docs-dashboard/` and runs `bun build` directly. Whether `bun build` resolves `workspace:*` packages without a workspace-root install is not addressed. The workspace root is `src/sdd/` (not `docs-mcp/`). A developer cannot know whether to add `bun install` at the workspace root, at `docs-dashboard/`, or whether Bun's bundler resolves workspace packages from existing `node_modules` automatically.
   - **Doc**: Build script step 2: `cd "$SRC/docs-mcp" && bun install --frozen-lockfile`. Step 3: `cd "$SRC/docs-dashboard" && bun build --target=bun src/server.ts ...` — no install step for docs-dashboard.
   - **Code**: `src/sdd/docs-dashboard/package.json` — `"dependencies": { "docs-mcp": "workspace:*" }`. Workspace root is `src/sdd/package.json` with `"workspaces": ["docs-mcp", "docs-dashboard", "docs-dashboard/ui"]`.

4. **GitHub Action `git push` has no branch or remote target specified** — the workflow step runs `git push` with no arguments on the CI runner. When CI checks out via `actions/checkout@v4`, the checkout is typically in detached HEAD or on the triggering branch without a configured push remote. The ADR does not specify: what `remote` and `ref` are pushed to, whether the `GITHUB_TOKEN` write permission needs to be declared (`permissions: contents: write`), and what happens on PRs from forks (where `GITHUB_TOKEN` has no push access). A developer implementing the workflow file cannot fill in the missing configuration without guessing.
   - **Doc**: GitHub Action step: `git push` (bare, no remote or ref). No `permissions:` key in the workflow YAML.
   - **Code**: No `.github/` directory exists yet — the file is new.

5. **Dashboard skill is not updated in the build — the source file that must change is in the built artifact** — The ADR states the `/dashboard` skill needs to reference `${CLAUDE_PLUGIN_ROOT}/dist/docs-dashboard.js` instead of `${CLAUDE_PLUGIN_ROOT}/docs-dashboard/src/server.ts`. The `/dashboard` skill lives at `src/sdd/.agent/skills/dashboard/SKILL.md` (the canonical source). The build step copies this file verbatim to `claude/sdd/skills/dashboard/SKILL.md`. If `build.sh` copies the skill file without modifying it, the installed skill still has the old TypeScript source path. The ADR does not say whether: (a) the canonical SKILL.md in `src/sdd/` is updated before building (implying developers must keep two divergent commands in mind), (b) `build.sh` post-processes the copied skill to substitute the path, or (c) the SKILL.md is supposed to contain a conditional that checks for the bundled path at runtime.
   - **Doc**: "The `/dashboard` skill needs to reference the bundled server instead of TypeScript source" / "Copy skills ... from `src/sdd/.agent/skills/`"
   - **Code**: `src/sdd/.agent/skills/dashboard/SKILL.md:55` — `bun run "${CLAUDE_PLUGIN_ROOT}/docs-dashboard/src/server.ts" --docs-dir "$CLAUDE_PROJECT_DIR/docs" --port $PORT`

6. **The `setup` skill exclusion logic skips `setup` unconditionally but the exclusion rationale conflicts with the copy target** — The build script comment says `[ "$name" = "setup" ] && continue` with the explanation "Don't overwrite real files (setup/SKILL.md is Claude-specific)". This implies `claude/sdd/skills/setup/SKILL.md` is the real file that must not be clobbered. However, `setup/` does not exist under `src/sdd/.agent/skills/` (confirmed: the directory listing shows no `setup` there), so the guard can never trigger — iterating over `src/sdd/.agent/skills/*/` will never yield `setup`. This is a benign no-op but it means a developer reading the build script cannot tell whether the skip is defensive (guarding against future addition of a `setup` skill in src) or a meaningful gate. More importantly, there is no step in the build script that validates that `claude/sdd/skills/setup/SKILL.md` actually exists before the build finishes — if it were accidentally deleted, the build would succeed silently and the installed plugin would have no `/setup` skill.
   - **Doc**: Build script step 5: `[ "$name" = "setup" ] && continue` with comment "Don't overwrite real files (setup/SKILL.md is Claude-specific)"
   - **Code**: `src/sdd/.agent/skills/` contains: `dashboard`, `design-audit`, `feature-change`, `file-bug`, `local-setup`, `plan-feature`, `spec-evaluator` — no `setup` directory. `claude/sdd/skills/setup/SKILL.md` is the only real file in `claude/sdd/skills/`.

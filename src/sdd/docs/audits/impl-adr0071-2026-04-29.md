# Implementation Compliance Audit — ADR-0071

**ADR**: `src/sdd/docs/adr-0071-dist-as-installable-artifact.md`
**Date**: 2026-04-29
**Evaluator**: implementation-evaluator subagent

## Checklist Results

| # | Item | Result | Notes |
|---|------|--------|-------|
| 1 | `build.go` exists and works | **PASS** | `src/sdd/build.go` and `src/sdd/go.mod` exist; `cd src/sdd && go run build.go` exits 0, produces dist/ for both platforms |
| 2 | `build.sh` deleted | **PASS** | `src/sdd/build.sh` does not exist |
| 3 | `--lint` mode works | **PASS** | `cd src/sdd && go run build.go --lint` exits 0 with "Lint PASSED" |
| 4 | src agents are body-only | **PASS** | All 4 files in `src/sdd/agents/*.md` start with blank line + `# Title`, no `---` YAML block |
| 5 | src skills are body-only | **PASS** | All 8 `src/sdd/skills/*/SKILL.md` files start with `#` or blank line, no `---` YAML block |
| 6 | Platform stubs are frontmatter-only (for .md files) | **PASS** | `factory/sdd/droids/*.md` all have frontmatter + `{{ include "agents/<name>.md" }}` body, no other content |
| 7 | Platform stubs are Go templates (for non-.md) | **PASS** | `factory/sdd/.factory-plugin/plugin.json` contains `{{ .Version }}`, `{{ .Description }}`, `{{ .BuildHash }}` |
| 8 | `context.md` stubs use include | **PASS** | Both `factory/sdd/context.md` and `claude/sdd/context.md` contain `{{ include "context.md" }}` |
| 9 | `dist/` is fully derived | **PASS** | Moved `factory/sdd/dist/` to /tmp, ran build, dist/ fully restored with all expected files |
| 10 | `dist/` plugin.json has correct version | **PASS** | `factory/sdd/dist/.factory-plugin/plugin.json` version=1.0.11 matches `src/sdd/version.json` version=1.0.11; buildHash matches |
| 11 | `.agent-templates/` deleted | **PASS** | `factory/sdd/.agent-templates/` does not exist |
| 12 | `local-setup` deleted | **PASS** | `src/sdd/skills/local-setup/` does not exist |
| 13 | setup skill created in src | **PASS** | `src/sdd/skills/setup/SKILL.md` exists with shared body content |
| 14 | Marketplace manifests updated | **PASS** | `.factory-plugin/marketplace.json` source = `./factory/sdd/dist`; `.claude-plugin/marketplace.json` source = `./claude/sdd/dist` |
| 15 | `.droid-plugin/` deleted | **PASS** | `.droid-plugin/` does not exist at repo root |
| 16 | CI workflow updated | **PASS** | `.github/workflows/build-plugin.yml` has `actions/setup-go@v5`, separate `lint` job running `go run build.go --lint`, build job running `go run build.go`, correct path triggers |
| 17 | Content hash (not git hash) | **PASS** | `build.go` uses `crypto/sha256` for content hashing; no `git rev-parse HEAD` anywhere in the file |
| 18 | hooks stubs are Go templates | **PASS** | `factory/sdd/hooks/hooks.json` uses `{{ if eq .Platform ... }}`; `workflow-reminder-hook.sh` uses `{{ .Platform }}` |
| 19 | guard stubs use include | **PASS** | All 3 guard stubs (`blocked-commands.sh`, `source-guard.sh`, `workflow-reminder.sh`) use `{{ include "hooks/guards/<name>.sh" }}` |
| 20 | Compiled artifact stubs exist | **PASS** | `factory/sdd/docs-mcp.js` contains `{{ include "docs-mcp.js" }}`; `factory/sdd/docs-dashboard.js` contains `{{ include "docs-dashboard.js" }}` |

## Overall Verdict

**CLEAN** — All 20 checklist items pass. ADR-0071 is fully implemented.

{
  "mcpServers": {
    "docs": {
      "type": "stdio",
      "command": "bun",
      "args": [
        "run",
        "{{ if eq .Platform "factory" }}${DROID_PLUGIN_ROOT}{{ else }}${CLAUDE_PLUGIN_ROOT}{{ end }}/dist/docs-mcp.js"
      ]
    }
  }
}
